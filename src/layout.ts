// Per-z visual Y-lift for board rendering. 0 = layers render flat.
// Shared by the play board (`components/Board.tsx`) and the editor
// (`editor/BoardCanvas.tsx`) so tuning here updates both.
export const LAYER_LIFT = 0;
